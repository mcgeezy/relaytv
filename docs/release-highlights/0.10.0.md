## Faster, more resilient live controls

RelayTV now prefers versioned WebSocket updates for the browser and X11
overlay, while preserving Server-Sent Events and HTTP polling fallbacks for
older companion clients and restrictive proxies.

---
